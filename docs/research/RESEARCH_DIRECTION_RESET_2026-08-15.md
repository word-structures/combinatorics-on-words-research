# RESEARCH DIRECTION RESET (2026-08-15)

## PRIMARY OBJECTIVE: STRUCTURAL UNDERSTANDING

**Mission:** Move from optimizing bounded searches toward discovering structural properties that could advance or decisively organize work on Mäkelä's conjecture. 

The optimization cycle is now closed. The following three programs represent the next phase of structural research.

## PROGRAM 1 — EXTENDABLE CORE

**Definition:**
`E_k(n)` = aa2f words of length `n` that admit an extension of `k` more letters.

Conceptually:
`E_infinity(n)` = intersection over `k` of `E_k(n)`.

**Question:**
Does finite-lookahead extendability exhibit stabilization structure?

**Cheap first probe:**
For small enumerable `n`, compute `|E_k(n)|` over increasing `k` and look for plateaus.

**CRITICAL CLAIM BOUNDARY:**
A finite observed plateau is NOT proof that `E_k = E_infinity`. A transfer/stabilization proof would still be required. The record hunter's new primary role is to generate dead-end/hard-state data relevant to this question.

## PROGRAM 2 — ROUTE-C UNIFORM OBSTRUCTION

Before any full L=6 Route-C campaign, measure:
- `S_large(4)`
- `S_large(5)`

under the exact existing repository definition. This closes the gap after the existing L=1, 2, 3 measurements. 

Later, if warranted, study recurring minimal order-sensitive/seam obstruction cores across `L`.

**Do NOT authorize L=6 exhaustive closure yet.**

## PROGRAM 3 — SOURCE MORPHISM / DIFFERENCE-LATTICE DIRECTION

Use the existing Stage-A linear obstruction viewpoint to ask which source morphisms could possibly admit a coding matrix escaping the known source difference lattice. 

This is the high-variance positive-construction direction. Do not launch broad morphism enumeration.

## THE RECORD HUNTER ROLE

The revised scientific role of the record hunter is, in order:
1. Hard-state / dead-end dataset generator
2. Structural-hypothesis falsifier
3. Minimal-dead-end finder
4. Obstruction-statistics generator
5. Record producer

*Record length is no longer the primary research objective.*
