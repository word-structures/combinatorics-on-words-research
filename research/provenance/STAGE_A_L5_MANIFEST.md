# STAGE_A_L5_MANIFEST.md — Stage-A L=5 survivor set, identity record

**Recorded:** 2026-08-14
**Purpose:** bind the identity of the L=5 Stage-A survivor artifact into version control,
independently of the machine and of the campaign directory that currently reference it.

**This manifest is text only. The artifact itself is not in Git and must not be added** —
it is 273 MB of derived intermediate data, regenerable from tracked source at the
configuration recorded below.

---

## 1. Artifact identity

| Field | Value |
|---|---|
| Logical name | `stageA_survivors.ndjson` |
| SHA256 | `5d85138a0c432ae1ba59a6734326e431de5c417a5d7abcae31e6f8850da4eca1` |
| Size | `272,918,577` bytes |
| Records | `5,153,928` (one JSON object per line) |
| Format | NDJSON, one surviving Stage-A profile tuple per record |

The hash was verified twice by streaming the file, on 2026-08-13.

## 2. Generator and configuration

| Field | Value |
|---|---|
| Generator | `scripts/parikh-block-filter.js`, driven by `stageA.js --full-l5` |
| Entry call | `generateStageAStream(5, 120, 9)` — `stageA.js:64` |
| Configuration | `L = 5`, `mMax = 120`, `iterN = 9` |
| Source morphism | `h₆`, 3-uniform, as defined in `src/morphisms.js` |
| Stage-A source word | `h₆⁹(a)` — 19,683 source symbols, **98,415** coded symbols at `L = 5` |
| Difference range | `m ∈ [2,120]`, i.e. block-aligned half-lengths `K = 5m ∈ [10,600]` |

## 3. Derived quantities

| Quantity | Value | Basis |
|---|---|---|
| Profile domain | `21⁶ = 85,766,121` | 21 = compositions of 5 into 3 parts |
| Raw coding family | `243⁶ = 205,891,132,094,649` | `3⁵ = 243` images per letter |
| Profiles eliminated | `80,612,193` | domain − survivors |
| Profiles surviving | `5,153,928` | the record count above |
| Concrete codings covered by survivors | `3,316,540,933,500` | multinomial mass over surviving profiles |

**Provenance note on the last figure.** It was **not** produced by
`scripts/parikh-block-filter.js` — that script's `multinomial4` helper returns `NaN` at
`L = 5` and prints `NaN` on its total-codings line. The figure comes from
`l5_accounting_and_strata.js` and was independently re-derived on 2026-08-13. See
`NEGATIVE_RESULTS.md` §24.

## 4. Relationship to the H4 campaign manifest

The L=5 closure campaign's `full_run_manifest.json`
(`C:\MSVC\L5_FULL_CLOSURE_CAMPAIGN_1\`, **outside this repository**) records
`stageASha256` equal to the SHA256 in §1, and the artifact hashes to exactly that value.
The running campaign therefore operates on precisely the survivor set accounted for here —
this is a verified identity, not an assumed correspondence.

Other fingerprints recorded by that manifest, reproduced here for cross-reference only:

| Field | Value |
|---|---|
| `coreFingerprintSha256` | `8ee27c558e1d8adbee3df437df36c321ac593d3b5ba3c52e637ffcff8bd42d8f` |
| `h6SourceSha256` | `02682f49e7b4980d5f310a917e9d0d7971cc8d9f71ef84e11a7e2df0023d77b0` |
| `planManifestSha256` | `fbfac5c4a34733b60253f9ff4a428eedafbdfa0318c130757ddd031beae9611b` |

## 5. Durability status

**AT RISK.** As of 2026-08-14 exactly **one** physical copy exists, at an untracked
worktree path:

```text
C:\abc-worktrees\ag-research-director-1\scratch\antigravity\a1-bounded-runner-1\outputs\stageA_survivors.ndjson
```

Integrity is covered — the hash above pins the bytes. Durability is not. If the file were
lost, the recorded hash would prove only that something specific is missing: the campaign's
result could no longer be re-verified against its own input without regenerating the file
and re-establishing that the regenerated bytes match.

**Recommendation:** take a second copy on separate physical media before the campaign
completes. This is an archival action for the owner; no copy is made by recording this
manifest.

## 6. What this manifest does and does not assert

- It **does** assert: these bytes, this size, this record count, this generator, this
  configuration, and this hash agreement with the campaign manifest.
- It **does not** assert that the survivor set is correct, complete, or minimal beyond
  what the Stage-A rule itself guarantees.
- It **does not** assert anything about L=5 closure. Survivorship at Stage A carries no
  positive avoidance guarantee — see `MATH_CLAIMS.md` rows 80 and 82.
