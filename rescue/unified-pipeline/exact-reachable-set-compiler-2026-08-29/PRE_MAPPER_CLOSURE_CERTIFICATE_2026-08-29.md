# PRE-MAPPER CLOSURE CERTIFICATE
**Date:** 2026-08-29

## 1. Sign Convention Lock
The convention $\sigma(X) + t = 0$ is fixed and verified across all theoretical markdown files and compiler tests. A strict audit confirmed 0 contradictory definitions of $t$ and the safe-elision requirement $-t \notin \mathcal{R}_\sigma(\rho)$.

## 2. Factor Language Closure
The target language factor set $F_5(h_6^\omega(a))$ was strictly derived from the ground up using the morphic boundary operator.
- $|F_2| = 14$ (closed, verified)
- $|F_3| = 22$ (closed, verified)
- $|F_5| = 38$ (closed, verified)
No finite iterates were used as a completeness premise.

## 3. Gate-0 Authoritative Manifest
`PAPER4_LAYER_C_INPUT_MANIFEST_2026-08-29.json` securely tracks the dependencies, capturing `sixdomain_full.js`, `sixdomain_full.json`, the 19-family distinctness parity checker, and its output log, alongside their exact SHA-256 hashes.

## 4. Separation of Independence Claims
Validation of Layer C was successfully split into two completely decoupled axes:

**A. Reachable-Set Engine Parity:**
The exact integer-arithmetic JS compiler was checked against the `reachable_set_bruteforce_oracle.py`. Over 21,900 compiled subsets across $L=2 \dots 8$ were exhaustively verified against literal string instantiations.
**Result:** 0 false members, 0 missing members.

**B. Physical Signature-Catalogue Parity:**
An independent implementation (`physical_catalogue_parity.js`) reconstructed the signature catalogue purely from physical cutpoint geometry ($u, v, w$) and simulated masking combinations, without calling the frozen Paper 4 library.
**Result:** Exact parity established ($L=5 \dots 8$). 0 missing physical signatures, 0 extra compiled signatures, 0 wrong family assignments.

## 5. FINAL VERDICT
**A. PRE-MAPPER CLOSURE COMPLETE — MAPPER AUTHORIZED**

The formal mathematical objects—reachable sets and physical signatures—are strictly certified. We are authorized to implement the dynamic topology mapper to connect the literal candidate window directly to the reachable set queries during the microbenchmark.
