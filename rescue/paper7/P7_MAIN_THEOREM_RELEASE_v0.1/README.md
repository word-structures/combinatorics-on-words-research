# P7 Main Theorem Release (v0.1)

### Established internally
`abacabadc` $\in re(\mathcal{A}_4) \setminus le(\mathcal{A}_4)$

### Consequence
$re(\mathcal{A}_4) \setminus e(\mathcal{A}_4) \neq \emptyset$

### Verification status
Computer-assisted proof with independent certificate replay.

### Novelty status
`NO PRIOR RESOLUTION FOUND — NOVELTY PROVISIONAL`

This package contains the complete theorem bundle, including formal definitions, base case datasets, and the minimal `verify_p7_main_theorem.js` certificate checker.
