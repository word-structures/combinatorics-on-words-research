diff --git a/MATH_CLAIMS.md b/MATH_CLAIMS.md
index ceb4123..88485f6 100644
--- a/MATH_CLAIMS.md
+++ b/MATH_CLAIMS.md
@@ -236,3 +236,8 @@ QUOTABLE_FACTS -->
 
 | 114 | **In the audited finite family of profile-class hard deletions for L_{h-1}->L_h, h=2,...,7, the 15 occurring canonical profile classes split as 6/6 minimum-B classes with delta_a>0 and 9/9 remaining classes with delta_a<0.** | Computation this project's own. Canonical evidence package: `research/verification/profile-response-h2-h7-2026-08-25/` (durable recovery RUN3C). | `COMPUTED` (Level 1 Internal Checksum) | 2026-08-25 | 2026-08-25 | **Bounded finite-family computational observation:** establishes the exact split for h=2,...,7 only. **What this does NOT establish:** novelty not established; not a universal law; not a theorem for arbitrary h; not a causal claim about B; not a prediction about h=8 (h=8 remained blind). Internal computational evidence only. |
 | 115 | **Exact spectrum of the h6 incidence matrix.** For the Rao-Rosenfeld source morphism h6 : a->ace, b->adf, c->bdf, d->bdc, e->afe, f->bce, the letter-incidence matrix M (entry [i][j] = occurrences of letter i in h6(j)) has characteristic polynomial det(xI - M) = x^6 - 3x^5 - 3x^4 + 9x^3 = x^3 (x-3) (x^2-3). Spectrum {3, sqrt(3), -sqrt(3), 0, 0, 0}; trace(M) = 3; rank(M) = 4. Hence the subdominant eigenvalue satisfies abs(lambda_2) = sqrt(3) > 1, and the discrepancy exponent is log(sqrt 3) / log 3 = 1/2. | This project's own exact computation; no external source consulted. Two independent derivations: Faddeev-LeVerrier integer recurrence (2026-08-29) and Leibniz permutation expansion over all 720 permutations with exact BigInt polynomial arithmetic (2026-08-30). Independence axes that differ: derivation, algorithm, data representation. Shared: the morphism definition itself, and the language (JS). | `COMPUTED` (Level 1 internal checksum) | 2026-08-30 | 2026-08-30 | **Scope:** an exact statement about one specific 6x6 integer matrix. Elementary linear algebra over the integers, recorded as internally verified arithmetic, NOT as a novel or externally sourced result; carries no literature-novelty implication. **Recorded near-miss:** a floating-point QR iteration first reported the spectrum as {3,0,0,0,0,0}, which would have made the source discrepancy bounded and killed the dependent research line on a false negative. The exact characteristic polynomial corrected it; trace and rank corroborate independently. This is the motivating case for the rule in `AGENTS.md` that a number gating a decision must have an exact route. **Does NOT claim** anything about Paper 4's classification theorem, whose statement and proof are independent of this matrix's spectrum. |
+| 116 (P7-M1) | The 9-symbol word `s = abacabadc` belongs to the four-letter Abelian-square-free language `A4`. | Internal direct checker: `CODEX_INDEPENDENT_AUDIT/P7_CODEX_CLEANROOM_VERIFIER.py`; manuscript v0.4 Section 3. | `COMPUTED` (`LEVEL_1_INTERNAL_CHECKSUM`) | 2026-09-03 | 2026-09-03 | Direct enumeration read from raw output: `S first square: None`. This reports no violation among all factors of this fixed 9-symbol word; it is not external source verification. Novelty status: `NOT_ESTABLISHED`. | `NONE` |
+| 117 (P7-M2) | `L_1(s)` is empty: none of `a`, `b`, `c`, or `d` can be prepended to `s = abacabadc` while preserving Abelian-square-freeness. | Four explicit witnesses in `P7_MANUSCRIPT_v0.4.md` Section 3; independently regenerated by `CODEX_INDEPENDENT_AUDIT/P7_CODEX_CLEANROOM_VERIFIER.py`. | `COMPUTED` (`LEVEL_1_INTERNAL_CHECKSUM`) | 2026-09-03 | 2026-09-03 | Raw outputs read: `aa` at start 0, half-period 1; `baba` at start 0, half-period 2; `cabacaba` at start 0, half-period 4; `dabacabadc` at start 0, half-period 5. Each displayed pair of halves has the same Parikh vector. Novelty status: `NOT_ESTABLISHED`. | `NONE` |
+| 118 (P7-M3) | For `C = abacabadcdb`, `W_0=C`, `W_{n+1}=C g_85(W_n)`, the nested right-infinite limit `W_infinity` is Abelian-square-free. | Proof in `P7_MANUSCRIPT_v0.4.md` Sections 4-8; external preservation dependency is ledger row 3; clean-room reconstruction in `CODEX_INDEPENDENT_AUDIT/P7_CODEX_HIGH_CERTIFICATE_RECONSTRUCTION.md`. | `COMPUTED` (`LEVEL_1_INTERNAL_CHECKSUM`) | 2026-09-03 | 2026-09-03 | Exact reconstruction, not finite extrapolation: 99 seed rows -> 35 residual states -> 17 recursive rows; all reconstructed sets match the supplied certificate; no transition source lies outside Q; minimum exact descent margin 79; direct base scan found no violation in the conservative 190-symbol prefix; the text proves universal invariant closure and applies the finite-factor argument. Raw output was read before this row was written. External `g_85` preservation remains separately `LEVEL_2_VERIFIED_SOURCE` in row 3. Novelty status: `NOT_ESTABLISHED`. | `PROVED MECHANISM` |
+| 119 (P7-M4) | `s = abacabadc` belongs to `re(A4) \ le(A4)`. | `P7_MANUSCRIPT_v0.4.md` Theorem 9.1; combines ledger rows P7-M2 and P7-M3. | `COMPUTED` (`LEVEL_1_INTERNAL_CHECKSUM`) | 2026-09-03 | 2026-09-03 | The explicit infinite word from P7-M3 begins with `s`, while P7-M2 excludes every one-letter left extension. This is a mathematical deduction from the registered components. Novelty status: `NOT_ESTABLISHED`; literature verdict is `NO PRIOR RESOLUTION FOUND - NOVELTY PROVISIONAL`. | `NONE` |
+| 120 (P7-M5) | `re(A4) \ e(A4)` is nonempty. | `P7_MANUSCRIPT_v0.4.md` Corollary 9.2; P7-M4 and Shur definitions. | `COMPUTED` (`LEVEL_1_INTERNAL_CHECKSUM`) | 2026-09-03 | 2026-09-03 | The witness from P7-M4 lies in `re(A4)` and outside `le(A4)`; the definition gives `e(A4) subseteq le(A4)`. The Shur primary-source definitions and inclusion were opened on 2026-09-03 and are recorded in the independent audit. Novelty status: `NOT_ESTABLISHED`; no claim of first solution is made. | `NONE` |
