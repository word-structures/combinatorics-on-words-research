# PAPER 6 — THEORY CORE STATUS v2.5
**Date:** 2026-08-30  
**Status:** main-theorem checkpoint

## Main exact theorem

For FULL L4 / Q2 the exact statewise continuation future has dimension

\[
\boxed{1179=12+1167}.
\]

For true-grid recency-gauged recent-block-profile measurements, the exact
observability indices are

\[
\boxed{
\nu_1=197,\quad
\nu_2=24,\quad
\nu_3=4,\quad
\nu_4=2.
}
\]

Family counts per time slice:

\[
6,\quad51,\quad345,\quad1796.
\]

For m=1,2,3, the indices attain the absolute row-count lower bounds:

\[
\left\lceil1179/6\right\rceil=197,
\quad
\left\lceil1179/51\right\rceil=24,
\quad
\left\lceil1179/345\right\rceil=4.
\]

Thus the coarse profile observations are time-optimal for depths 1--3.

## Four-profile anomaly

The true-grid four-profile measurement is exactly rank

\[
1144
\]

over \(\mathbb Q\), so its hidden sector has exact dimension

\[
35.
\]

But

\[
\operatorname{rank}_{\mathbb Q}
\begin{bmatrix}
M_4\\M_4Q
\end{bmatrix}
=1179.
\]

Hence every statically hidden direction is exposed after one block step:

\[
H_4\cap\ker(M_4Q)=\{0\}.
\]

This gives exact observability index 2.

## Connection to v2.4

The exact 35-row minimal static phase repair and the one-step delayed
measurement are two coordinate systems that resolve the same 35-dimensional
latent future sector.

The 15/16 anchor-policy theorem remains valid but is now supporting evidence,
not the central narrative.

## Revised central interpretation

Paper 6 should no longer ask only:

> how many suffix/history coordinates are required?

The exact Q2 theorem demonstrates a space--time tradeoff:

\[
\boxed{
\text{less structural detail per observation}
\quad\Longleftrightarrow\quad
\text{more observation times}.
}
\]

This is a much more robust statement than privileging one static descriptor.

## Novelty discipline

Observability matrices/indices and finite-dimensional linear systems are
classical.

The candidate contribution is the exact Abelian selected-library
space--time spectrum, semantic hierarchy, one-step latent sector, minimal
static repair and counterexamples to naive deterministic state compression.

## Manuscript readiness consequence

There is now a coherent finite-system main theorem package.

A universal theorem predicting the spectrum from L,Q,B would strengthen the
paper further but is no longer required to prevent the manuscript from being a
collection of disconnected computational observations.

## Next mathematical target

Attempt to predict the observability index from Abelian geometry and profile
family counts across Q/L/libraries.

The immediate falsifiable question is whether the row-count-optimal phenomenon
for m=1--3 replicates in another selected library or block length.
