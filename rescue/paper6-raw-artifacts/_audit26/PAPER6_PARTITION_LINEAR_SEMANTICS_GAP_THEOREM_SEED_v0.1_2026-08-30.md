# PAPER 6 — PARTITION–LINEAR SEMANTICS GAP THEOREM SEED v0.1
**Date:** 2026-08-30  
**Status:** exact finite-system theorem/certificate; general linear facts are classical

## Executive result

For the full L4 aa2fr block-range Q2 system, exact future counting has two
fundamentally different notions of complexity.

### State-merging complexity

There are

\[
\boxed{2689}
\]

distinct all-horizon continuation-count behaviors among the 2691 stable
weighted/equitable classes.

### Exact linear future complexity

The vector Krylov space

\[
V_{\rm cnt}
=
\operatorname{span}_{\mathbb Q}
\{
\mathbf1,Q\mathbf1,Q^2\mathbf1,\ldots
\}
\]

has dimension exactly

\[
\boxed{1179}.
\]

Therefore

\[
\boxed{
2689\ \text{count states}
\quad\text{vs.}\quad
1179\ \text{exact linear future dimensions}.
}
\]

This is not merely a scalar-initial-sequence phenomenon.

The same degree-1179 integer polynomial annihilates the complete
2691-coordinate statewise future-count vector sequence.

---

# 1. Definitions

Let \(Q\) be the exact finite weighted transition matrix.

For each state \(i\), define

\[
c_n(i)=e_i^\top Q^n\mathbf1.
\]

Two states are **count equivalent** when

\[
c_n(i)=c_n(j)
\qquad
\text{for every }n\ge0.
\]

The count quotient therefore groups identical rows of the infinite future
matrix

\[
\mathcal F=
\big[
\mathbf1,\,
Q\mathbf1,\,
Q^2\mathbf1,\,
\ldots
\big].
\]

Its number of classes is the number of distinct rows of \(\mathcal F\).

By contrast, the exact linear future dimension is

\[
\operatorname{rank}_{\mathbb Q}\mathcal F.
\]

Distinct-row count and matrix rank are different invariants.

---

# 2. Exact Q2 count-state result

The stable weighted/equitable quotient has

\[
2691
\]

classes.

There are exactly two nontrivial all-horizon count merges:

\[
\{224,1021\},
\qquad
\{1645,2244\}.
\]

Hence

\[
\boxed{
N_{\rm count}=2689.
}
\]

The global count-signature class counts by horizon are

\[
\boxed{
1,\ 22,\ 536,\ 2336,\ 2684,\ 2689,\ 2689,\ldots
}
\]

Thus the finite signatures

\[
(c_0,c_1,\ldots,c_5)
\]

already distinguish all 2689 infinite count behaviors in this particular
system.

This finite distinguishing horizon is an empirical/exact Q2 calibration, not a
general fixed-horizon theorem.

---

# 3. Exact vector Krylov certificate

The independently certified scalar recurrence has order

\[
1179
\]

with connection coefficients

\[
C_0=1,C_1,\ldots,C_{1179}.
\]

Its convention is

\[
s_n+\sum_{i=1}^{1179}C_i s_{n-i}=0.
\]

Define the corresponding operator polynomial

\[
p(x)
=
x^{1179}
+
C_1x^{1178}
+\cdots+
C_{1179}.
\]

A new exact integer replay evaluated

\[
\boxed{
p(Q)\mathbf1.
}
\]

The calculation used the saved sparse 2691-state exact quotient and integer
arithmetic.

Result:

\[
\boxed{
p(Q)\mathbf1=0
}
\]

in **all 2691 coordinates**.

Nonzero residual coordinates:

\[
\boxed{0}.
\]

The largest sampled intermediate continuation counts had 674 decimal digits,
so this was not a floating-point or small-number identity.

---

# 4. Exact dimension proof

The vector polynomial relation gives

\[
\dim V_{\rm cnt}\le1179.
\]

Independently, the scalar assembly-count sequence has exact Hankel rank 1179.

Any linear dependence among

\[
\mathbf1,Q\mathbf1,\ldots,Q^{1178}\mathbf1
\]

would induce a recurrence of order below 1179 for every left projection,
including the certified scalar assembly sequence.

Therefore

\[
\dim V_{\rm cnt}\ge1179.
\]

Combining the bounds:

\[
\boxed{
\dim_{\mathbb Q}V_{\rm cnt}=1179.
}
\]

This is an exact rational result.

---

# 5. Consequence — exact statewise linear realization

Every statewise continuation-count sequence

\[
(c_n(i))_{n\ge0}
\]

is generated by the same order-1179 recurrence.

Equivalently, the entire vector sequence

\[
Q^n\mathbf1
\]

admits an exact 1179-dimensional linear realization.

No linear realization generating the complete vector future can have smaller
dimension.

Thus 1179 is not just:

> the complexity of one initial scalar sequence.

It is:

> **the minimal exact linear dimension of the complete all-state
> continuation-count future generated from the terminal observable
> \(\mathbf1\).**

---

# 6. Deterministic merging versus signed linear combination

A partition quotient can only identify histories whose future sequences are
literally equal.

That leaves 2689 count classes.

A linear realization may instead exploit relations among different future
sequences.

It can use signed linear combinations and therefore needs only 1179
dimensions.

The reduction factor is

\[
\boxed{
2689/1179\approx2.28.
}
\]

Relative to the stable weighted/equitable state count:

\[
2691/1179\approx2.28.
\]

This is the clearest current demonstration that **state minimization and
future-dynamics minimization are different Paper-6 problems**.

---

# 7. Structural partition no-go

Consider the current richest static P4/P5-style descriptor tested:

- every affine geometry keeps its own selected-library obstruction mask;
- unavailable geometries are marked distinctly;
- the ordered profiles of all currently available complete blocks are retained
  (up to five at Q2).

This gives

\[
\boxed{2589}
\]

descriptor classes.

It is still unsound for exact counting:

- 96 descriptor groups merge different exact count classes;
- 198 equitable classes participate in those unsound groups.

Furthermore, both genuine exact count merges

\[
(224,1021),
\qquad
(1645,2244)
\]

are **already separated by the descriptor**.

Therefore any algorithm that begins from this descriptor and only
**refines/splits classes** can never produce the minimal 2689 count partition.

This is a logical no-go independent of runtime.

---

# 8. Dynamic refinement of the structural descriptor

Starting from the 2589 structural types:

### One stable weighted successor refinement

gives immediately

\[
\boxed{
2589\to2691.
}
\]

The descriptor reconstructs the full equitable quotient after one positive
weighted refinement.

### Scalar future moments

If instead the descriptor is augmented only with continuation counts:

\[
2589
\to
2642
\to
2688
\to
2691
\]

after including count horizons through

\[
2,\ 3,\ 4.
\]

It still ends at 2691 rather than 2689 because the two true count merges were
structurally separated from the start.

Thus:

\[
\boxed{
\text{positive descriptor refinement}
\neq
\text{minimal count semantics}.
}
\]

---

# 9. Architectural consequence

Paper 4 / Paper 5 structural information should not be treated as a partition
that Paper 6 merely refines until convergence.

Its stronger role is:

\[
\boxed{
\text{structural compiler / feature basis / candidate generator}.
}
\]

Paper 6 must then allow **signed linear recombination across structural
classes**.

The correct architecture is:

\[
\text{P4/P5 structural features}
\]

\[
\downarrow
\]

\[
\text{exact transfer action}
\]

\[
\downarrow
\]

\[
\boxed{
\text{Krylov/Hankel linear future realization}
}
\]

rather than

\[
\text{descriptor partition}
\to
\text{split}
\to
\text{split}
\to
\text{final quotient}.
\]

---

# 10. Relation to signed response cocycles

The two Q2 count merges are already the simplest examples of why signed
linear semantics is necessary.

Their positive successor distributions differ, so equitable semantics keeps
them separate.

But their signed response defects annihilate the future-count space.

The 1179-dimensional result is the global form of the same principle:

\[
\boxed{
\text{future counting is controlled by a linear subspace, not merely by a
partition of histories}.
}
\]

---

## Verdict

**This is a central Paper-6 structural result.**

At Q2:

\[
\boxed{
2691
\to
2689
\quad\text{by exact state merging},
}
\]

but

\[
\boxed{
2691
\to
1179
\quad\text{by exact linear future semantics}.
}
\]

The next universal solver should therefore target a direct structural
construction of the 1179-type future space (and its Q-dependent analogues),
not a better static history partition.
