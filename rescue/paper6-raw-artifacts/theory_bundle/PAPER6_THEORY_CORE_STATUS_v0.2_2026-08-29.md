# PAPER 6 — THEORY CORE STATUS v0.2
**Date:** 2026-08-29  
**Status:** active research checkpoint; not a manuscript

## Current central question

> **What is the smallest exact future-dynamics representation generated by
> Abelian obstruction geometry for a selected block language?**

Record hunting is now an application, not the scientific core.

## Results currently surviving

1. Relative selected-library survival entropy is well-defined.
2. Block-window and half-period cutoff hierarchies converge monotonically.
3. Equal-length block profiles give a universal additive-square obstruction.
4. Same profile and same one-step literal response are not sufficient counting
   states.
5. Stable weighted/counting quotients can be much smaller than literal states,
   but the amount of state merging is library-dependent.
6. Exact linear future spaces can be smaller still and remain substantially
   compressed even in an asymmetric library control.
7. Abelian boundary legality is given by the unified affine equation
   \[
   \Delta_{k,j}=0.
   \]
8. Keeping only currently active obstructions fails because of latent
   obstruction memory.
9. Keeping every raw obstruction coordinate can simply re-encode the whole
   suffix.
10. Static degree-2 interaction expansion of obstruction coordinates also
    fails as an explanation because it nearly reconstructs the raw state space.
11. The counting Krylov representation splits exactly into nilpotent transient
    and persistent future components.
12. Any selected library has the exact operator representation
    \[
    M_{B,K}=\sum_{w\in B}A_w.
    \]
13. Complete libraries satisfy
    \[
    M_{\rm full,K}=A^L.
    \]
14. Profile-conditioned transfer operators are coefficients of
    \[
    (x_aA_a+x_bA_b+x_cA_c)^L.
    \]

## Best current conceptual architecture

\[
\text{Abelian character safety}
+
\text{selected-library recognizer}
\]

\[
\downarrow
\]

\[
\text{profile/noncommutative block transfer operators}
\]

\[
\downarrow
\]

\[
\text{future semantics}
\]

\[
\downarrow
\]

\[
\text{persistent Krylov/Hankel dynamics}
\]

\[
\downarrow
\]

\[
\text{relative survival entropy}.
\]

Paper 4 supplies boundary/cutpoint geometry.

Paper 5 should supply family-level evaluation/reachable response.

Paper 6 should identify and compute the exact invariant future dynamics.

## Next gate P6-C5

The next highest-value task is:

> **construct the selected-library transfer operator from a compressed
> obstruction/family representation without first constructing all literal
> safety suffix states.**

Two attack routes should be compared.

### Route A — observable closure

Start with the finite family of Abelian boundary hyperplane indicators and
close only those combinations actually generated by block shifts and profile
aggregation.

Do not add arbitrary coordinate interactions.

### Route B — library-recognizer × structural-safety product

Represent the selected block language by a trie/DFA/profile generator and
combine it directly with Paper-4/Paper-5 structural safety states.

The decisive result would be a representation whose dimension/cost is governed
by obstruction families and library structure rather than by the number of
literal suffixes or literal blocks.

## Current no-go claims

Do not claim:

- profile alone is a counting state;
- one-step response alone is a counting state;
- counting-equivalence merging is always large;
- the complete obstruction coordinate tuple is compressed;
- pairwise obstruction features explain the low future rank;
- cross-block-length future dimension is invariant.

## Current strongest target statement

> **Selected Abelian-avoiding block languages admit a certified transfer
> hierarchy in which block selection is separated algebraically from safety,
> profile information is an exact weighted factor, and the remaining research
> problem is to construct the persistent future operator directly from the
> finite obstruction geometry.**
