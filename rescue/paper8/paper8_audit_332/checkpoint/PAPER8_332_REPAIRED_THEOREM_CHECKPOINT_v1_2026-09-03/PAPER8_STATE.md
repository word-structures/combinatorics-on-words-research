# Paper 8 state — repaired 332 checkpoint

## Promoted inside this checkpoint

For H8 profile `v=(3,3,2)`, the repaired proof architecture gives an internal computer-assisted continuum sign certificate

`C_332(x) < 0` for all `x in [0,1]`.

The explicit certified margin is

`C_332(x) < -563534714369/4860000000000 ≈ -0.11595364493`.

Consequently, because restoring target-edge weight has `d log(lambda)/dx > 0`, the asymptotic-variance path satisfies `da/dx < 0`; hence hard deletion has positive response for this profile.

## What changed from v4

The v4 generic `4 K tau^B` finite-to-infinite lemma was red-teamed and rejected as a general argument. It is not used here.

The replacement uses:

- giant-SCC boundary regularization;
- exact burned scored-window polynomial;
- one-block projective boundary certificate;
- Dobrushin contraction only on unscored burn zones;
- explicit polynomial-prefactor stationary kernel tail.

## Still open

- external audit of interval arithmetic / directed rounding for the one-block projective certificate;
- applying the repaired architecture to `(4,2,2)`, `(4,3,1)`, `(5,2,1)`;
- a repaired 4/4 H8 ordering theorem;
- novelty/prior-art status;
- H9.
