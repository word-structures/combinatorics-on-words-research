# Independent audit route

## Audit A — provenance / artifact integrity

- Verify `archives/ARCHIVE_SHA256SUMS.txt`.
- Run `RUN_MASTER_AUDIT.py`.
- Confirm the historical checkpoint hashes match the archived manifests.
- Confirm v2/v3/v4 are preserved rather than rewritten after the tail red-team.

## Audit B — H8 discovery reconstruction

From v2 and the supporting discovery report:

- reconstruct the H8 profile family;
- recompute the L7 baseline and hard-deletion responses;
- compare Green--Kubo/Perron and independent moment-growth DP;
- recompute the direct finite-dimensional S3 resolvent curve;
- verify the finite H2--H8 sign table is treated only as a finite observation.

## Audit C — exact finite-context arithmetic

For 332 and preferably one positive profile:

- rebuild the edge/size export from the stored quotient;
- recompute target-degree caps;
- rebuild GMP exact polynomials;
- independently reproduce modular coefficient checks;
- reproduce the global Bernstein/integer sign certificates.

## Audit D — rejected-tail red-team

Independently verify that the old v4 generic `4 K tau^B` derivation is incomplete.  The purpose is to prevent an auditor from accidentally validating a later repaired theorem by relying on the old lemma.

## Audit E — repaired 332 mathematics

Line-by-line verify:

1. giant-SCC boundary regularization;
2. the one-block projective ratio enclosure;
3. the identity evolving normalized boundary likelihood ratios by forward/reverse Parry kernels;
4. Dobrushin projective range contraction through the unscored burn blocks;
5. conversion from endpoint density-ratio bounds to the burn expectation error;
6. the stationary kernel decomposition and the shell-by-shell polynomial-prefactor tail;
7. the final triangle inequality and exact rational margin.

## Audit F — interval arithmetic

Reimplement the one-block projective certificate using a trusted directed-rounding or ball-arithmetic package.  Do not merely rerun the same IEEE-padding code.

The current theorem constants deliberately have slack (`rho_L <= 1.07`, `rho_R <= 1.10`) over the stored interval maxima, so this is an independent correctness check, not a request to match the last decimals.

## Audit G — propagate repair only after 332 audit

Only after repaired 332 survives independent audit, port the same architecture to 422, 431, and 521.  Rebuild a repaired 4/4 H8 theorem/order checkpoint from those results rather than reviving v4's old continuation step.

## Audit H — novelty

After correctness closes, perform a dedicated prior-art search around:

- pressure/susceptibility derivatives of finite-state symbolic systems;
- Markov holes / forbidden patterns;
- Dobrushin/Doeblin comparison bounds;
- low-rank pattern defects / return kernels;
- Abelian profile geometry and fluctuation response.
