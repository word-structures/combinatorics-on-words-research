# Paper 4 — general FAF–AFE theorem package

Independent of the running preregistered population experiment.

Main result:
- S_AFE(L) is contained in S_FAF(L).
- The exact excess is the same-parity midpoint family.
- Its size is floor(L^2/4).
- The combined gate is a shared-support / enlarged-target-set system.

Run:
```powershell
python verify_faf_afe_general_theorem.py --L 40
python verify_faf_afe_general_theorem.py --range 4 200
```

Do not promote to canonical Paper 4 before independent replay and novelty audit.
